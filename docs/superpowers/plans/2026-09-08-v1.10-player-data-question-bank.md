# 草人杯 1.10 人物数据模型与题库计算实施计划

> **执行要求：** 实施时使用 `superpowers:executing-plans`，由当前主代理按任务顺序执行并在每个检查点暂停复核。未经用户明确同意不得创建子代理。步骤使用复选框跟踪。

**目标：** 在不改变原版猜选手题库含义的前提下，为 1.10 建立稳定人物身份、可追溯历史事实、版本化资格计算、审核发布、缓存和 API 底层。

**架构：** 保留 `players.id`、`difficulty_levels` 和 `player_difficulties` 作为原版兼容层，在其旁边新增人物 UID、身份、证据、历史事实、不可变数据版本和物化题库集合。数据库是事实来源，Redis 只负责通知和加速；所有题库查询必须锁定 `version_uid`。

**技术栈：** Node.js 26、TypeScript 5.7、Express 4、Knex 3、Zod 3、PostgreSQL、SQLite、Redis 7.4、Vitest 2、pnpm 11.13.1。

## 全局约束

- 工作树固定为 `D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg`，分支固定为 `feat/v1.10-friberg`。
- 实施前再次读取 `D:\OpenSourcework\AGENTS.md`、`agent-rules/file-safety.md`、`agent-rules/testing.md` 和 `agent-rules/worktrees-and-branches.md`；内容未变化时不重复读取。
- 每次修改现有文件前先创建被 Git 忽略的 `.bak-20260908-v110-question-bank` 备份；新增文件没有旧内容，不创建空备份。
- 所有文本编码使用 UTF-8；不得提交备份、数据库、日志、构建产物或环境配置。
- 不修改 1.9.x 主工作区、异能工作树、ParticleMenu 或 Diagnostics。
- 不提交、不推送、不创建 PR、不合并、不部署、不发布数据版本，除非用户另行明确授权。
- 不连接 HLTV、Valve、GitHub 或其他外部服务；本阶段测试使用本地合成夹具。
- 原版 `beginner`、`easy`、`normal` 与新版 `simple`、`normal`、`hard`、`expert` 不得混用。
- 每个任务先写失败测试，再写最小实现，再运行直接相关验证，最后检查差异和意外文件。
- 当前本机没有 Redis 7.4 和 PostgreSQL；依赖这些服务的验证必须标记为待 CI 或待用户提供环境，不得声称本地通过。
- 所有本地集成测试必须显式把 `DB_URL` 指向当前任务专用的测试数据库，不得迁移或写入现有 `friberg-game/server/data/csgofriberg.sqlite3`；Task 2 使用 `friberg-game/.v110-question-bank-test-task2.sqlite3`，后续新增迁移时分别使用新的 `task3`、`task4` 等明确文件名。测试文件受 `friberg-game/.gitignore` 的 `*.sqlite3` 规则保护。
- 执行环境若拒绝删除旧测试数据库，不得绕过策略；保留其忽略状态并换用新的任务专用文件。
- 计划中的提交步骤因用户限制全部省略；每个任务只形成可审查的工作区检查点。

每次开始新的 PowerShell 测试会话时，先在工作树根目录执行：

```powershell
$portableNode = (Resolve-Path -LiteralPath 'node_modules\portable-node26\node-v26.8.1-win-x64').Path
$env:PATH = "$portableNode;$env:PATH"
$v110TestDb = Join-Path (Resolve-Path -LiteralPath 'friberg-game').Path '.v110-question-bank-test-task2.sqlite3'
$env:DB_CLIENT = 'sqlite'
$env:DB_URL = $v110TestDb
$env:REDIS_PREFIX = 'csgofriberg:v110-question-bank-test:'
```

随后再执行各任务列出的 Vitest 命令。若测试进程不是从同一 PowerShell 会话启动，必须在该进程命令前重复设置这三个环境变量。

---

## 文件结构

### 数据库

- `friberg-game/server/src/db/migrations/types.ts`：迁移接口。
- `friberg-game/server/src/db/migrations/index.ts`：有序迁移注册表与执行器。
- `friberg-game/server/src/db/migrations/001-person-identity.ts`：人物 UID、别名、外部身份、职业身份和 SQLite `players` 重建。
- `friberg-game/server/src/db/migrations/002-question-bank-facts.ts`：证据、导入批次、战队、榜单、成员、Major、贴纸和比赛准入事实。
- `friberg-game/server/src/db/migrations/003-question-bank-versions.ts`：版本快照、资格、成员、校验、审计和正式指针。

### 领域服务

- `friberg-game/server/src/services/questionBank/types.ts`：稳定领域类型和 API 输入输出。
- `friberg-game/server/src/services/questionBank/identity.ts`：UID、别名、外部身份和同名解析。
- `friberg-game/server/src/services/questionBank/evidence.ts`：候选证据和事实修订写入。
- `friberg-game/server/src/services/questionBank/qualification.ts`：纯资格计算器。
- `friberg-game/server/src/services/questionBank/materialization.ts`：五个基础集合与一致性检查。
- `friberg-game/server/src/services/questionBank/versionBuilder.ts`：不可变版本快照和变化报告。
- `friberg-game/server/src/services/questionBank/publication.ts`：审核、发布、回滚和审计。
- `friberg-game/server/src/services/questionBank/repository.ts`：指定版本的数据库读取。
- `friberg-game/server/src/services/questionBank/cache.ts`：进程缓存与 Redis 通知。
- `friberg-game/server/src/services/questionBank/catalog.ts`：统一题库解析与公开目录。

### 路由

- `friberg-game/server/src/routes/questionBank.ts`：公开题库目录。
- `friberg-game/server/src/routes/adminQuestionBank.ts`：管理端候选、审核、构建、发布和回滚。
- `friberg-game/server/src/routes/externalQuestionBank.ts`：外部 Token 候选批次提交。

### 测试

- `friberg-game/server/test/db/migrations.test.ts`
- `friberg-game/server/test/db/migrations.postgres.test.ts`
- `friberg-game/server/test/services/questionBank/identity.test.ts`
- `friberg-game/server/test/services/questionBank/evidence.test.ts`
- `friberg-game/server/test/services/questionBank/qualification.test.ts`
- `friberg-game/server/test/services/questionBank/materialization.test.ts`
- `friberg-game/server/test/services/questionBank/versionBuilder.test.ts`
- `friberg-game/server/test/services/questionBank/publication.test.ts`
- `friberg-game/server/test/services/questionBank/cache.test.ts`
- `friberg-game/server/test/services/questionBank/postgres.integration.test.ts`
- `friberg-game/server/test/routes/questionBank.integration.test.ts`
- `friberg-game/server/test/routes/adminQuestionBank.integration.test.ts`
- `friberg-game/server/test/routes/externalQuestionBank.integration.test.ts`
- `.github/workflows/friberg-game.yml`：增加 PostgreSQL 16 专用迁移与题库集成验证。

---

### Task 1：有序迁移框架与人物 UID 兼容迁移

**文件：**

- 新建：`friberg-game/server/src/db/migrations/types.ts`
- 新建：`friberg-game/server/src/db/migrations/index.ts`
- 新建：`friberg-game/server/src/db/migrations/001-person-identity.ts`
- 新建：`friberg-game/server/test/db/migrations.test.ts`
- 新建：`friberg-game/server/test/db/init.test.ts`
- 修改：`friberg-game/server/test/db/ready.test.ts`
- 修改：`friberg-game/server/test/db/seedPlayers.test.ts`
- 修改：`friberg-game/server/src/db/init.ts`
- 修改：`friberg-game/server/src/db/ready.ts`
- 修改：`friberg-game/server/src/db/seedPlayers.ts`

**接口：**

```ts
export interface DatabaseMigration {
  id: string;
  checksum: string;
  sqliteForeignKeysOff?: boolean;
  up(instance: Knex | Knex.Transaction): Promise<void>;
}

export async function runMigrations(
  instance: Knex = db,
  migrations?: readonly DatabaseMigration[]
): Promise<string[]>;

```

- [x] **Step 1：备份将修改的现有文件**

```powershell
Copy-Item -LiteralPath 'friberg-game/server/src/db/init.ts' -Destination 'friberg-game/server/src/db/init.ts.bak-20260908-v110-question-bank'
Copy-Item -LiteralPath 'friberg-game/server/src/db/ready.ts' -Destination 'friberg-game/server/src/db/ready.ts.bak-20260908-v110-question-bank'
Copy-Item -LiteralPath 'friberg-game/server/src/db/seedPlayers.ts' -Destination 'friberg-game/server/src/db/seedPlayers.ts.bak-20260908-v110-question-bank'
Copy-Item -LiteralPath 'friberg-game/server/test/db/ready.test.ts' -Destination 'friberg-game/server/test/db/ready.test.ts.bak-20260908-v110-question-bank'
Copy-Item -LiteralPath 'friberg-game/server/test/db/seedPlayers.test.ts' -Destination 'friberg-game/server/test/db/seedPlayers.test.ts.bak-20260908-v110-question-bank'
git status --short --ignored
```

预期：五个备份显示为 `!!`，不显示为可提交文件。

- [x] **Step 2：编写迁移失败测试**

在 `migrations.test.ts` 建立旧版内存 SQLite 结构和历史引用，至少断言：

```ts
expect(await instance.schema.hasTable('schema_migrations')).toBe(false);
await runMigrations(instance);
expect(await instance.schema.hasTable('schema_migrations')).toBe(true);
expect(await instance.schema.hasColumn('players', 'person_uid')).toBe(true);
expect(await instance.schema.hasTable('person_aliases')).toBe(true);
expect(await instance.schema.hasTable('person_external_identities')).toBe(true);
expect(await instance.schema.hasTable('person_career_roles')).toBe(true);
expect(await instance.schema.hasTable('identity_review_cases')).toBe(true);
expect(new Set(await instance('players').pluck('person_uid')).size).toBe(2);
expect((await instance('games').first()).target_player_id).toBe(originalPlayerId);
```

再加入：重复执行只记录一次迁移、不同人物允许同昵称、启用的原版人物不能缺少必要字段、失败迁移不写完成记录。

更新 `seedPlayers.test.ts` 的失败断言：迁移后的空库执行 `insertMissingSeedPlayers()` 能写入 5 人，每人都有不同的非空 UID，且身份状态为 `candidate`。

- [x] **Step 3：运行测试并确认失败原因正确**

```powershell
Set-Location 'friberg-game'
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/db/migrations.test.ts --no-file-parallelism
```

预期：因迁移模块和新表尚不存在而失败，不应出现 Node 版本或模块安装错误。

- [x] **Step 4：实现迁移执行器**

`runMigrations()` 必须：

1. 创建 `schema_migrations(id, checksum, started_at, completed_at)`；
2. 按迁移 ID 排序；
3. 对每条未完成迁移开启事务；
4. 校验同 ID 的 checksum 不可变化；
5. `up()` 成功后写完成记录；
6. 返回本次实际执行的迁移 ID；
7. 失败时回滚当前迁移并抛出原错误。

在 `initDb()` 中固定顺序：

```ts
await ensureSchema();
await runMigrations();
const seeded = await seedPlayersIfEmpty();
```

- [x] **Step 5：实现 `001-person-identity`**

迁移必须创建设计文档第 6 节的四张新表，并完成：

- 保留全部原 `players.id`；
- 用注入的 UID 生成器逐行回填 UUID；
- 把现有昵称写为 `primary` 别名；
- 添加 `identity_status` 和 `merged_into_player_id`；
- 移除昵称全局唯一约束；
- 允许候选人物的旧资料字段为 `NULL`；
- 约束 `is_enabled=true` 的人物具有原版比较所需字段；
- PostgreSQL 删除昵称唯一约束并修改列；
- SQLite 使用事务内新表复制、逐 ID 核对、外键检查和替换。

`person_external_identities` 对 `(source, identity_type, external_id)` 建唯一约束；`person_aliases` 对 `(player_id, normalized_alias)` 建唯一约束，但不对别名跨人物唯一。

- [x] **Step 6：更新就绪检查**

`assertDatabaseReady()` 增加人物身份表及列。现有 `Player` 类型保持到 Task 2；Task 2 将可空类型与原版缓存守卫一起修改，避免中间检查点让原版代码面对不完整人物。

`seedPlayers.ts` 在目标表已有 `person_uid` 时为新种子生成 UUID，并写入 `identity_status=candidate`；迁移前旧结构仍保持原插入格式。这样 `initDb()` 可以继续使用 `ensureSchema() → runMigrations() → seedPlayersIfEmpty()`，且空库和升级库都能运行。

- [x] **Step 7：运行迁移测试和旧数据库测试**

```powershell
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/db/migrations.test.ts test/db/schema.test.ts test/db/ready.test.ts test/db/seedPlayers.test.ts --no-file-parallelism
```

预期：全部通过；迁移测试证明 ID、外键和旧难度成员不变。

- [x] **Step 8：检查 Task 1 差异并暂停**

```powershell
Set-Location '..'
git diff --check
git status --short --ignored
git diff --stat
```

检查点：只包含 Task 1 文件和被忽略备份；不得出现数据库文件、构建产物或其他工作树改动。

---

### Task 2：人物身份服务与旧接口同名兼容

**文件：**

- 新建：`friberg-game/server/src/services/questionBank/types.ts`
- 新建：`friberg-game/server/src/services/questionBank/identity.ts`
- 新建：`friberg-game/server/src/services/questionBank/normalization.ts`
- 新建：`friberg-game/server/test/services/questionBank/identity.test.ts`
- 新建：`friberg-game/server/test/services/playerMutations.identity.test.ts`
- 新建：`friberg-game/server/test/services/playerChangeSubmissions.identity.test.ts`
- 新建：`friberg-game/server/test/services/playerCache.identity.test.ts`
- 修改：`friberg-game/server/src/services/playerMutations.ts`
- 修改：`friberg-game/server/src/services/playerChangeSubmissions.ts`
- 修改：`friberg-game/server/src/services/playerExport.ts`
- 修改：`friberg-game/server/src/services/playerCache.ts`
- 修改：`friberg-game/server/src/db/seedPlayers.ts`
- 修改：`friberg-game/server/test/routes/admin.integration.test.ts`
- 修改：`friberg-game/server/test/routes/playerChangeSubmissions.integration.test.ts`
- 修改：`friberg-game/server/test/routes/externalPlayers.integration.test.ts`
- 修改：`friberg-game/server/test/services/playerCache.test.ts`

**接口：**

```ts
export type PersonUid = string;
export type IdentityStatus = 'candidate' | 'verified' | 'conflict' | 'merged';

export function normalizePersonAlias(value: string): string;
export function isLegacyGuessReady(player: Player): player is LegacyGuessPlayer;
export async function createCandidatePerson(input: CreateCandidatePersonInput): Promise<number>;
export async function resolveLegacyImportTarget(input: {
  personUid?: string;
  nickname?: string;
}): Promise<{ kind: 'new' } | { kind: 'existing'; playerId: number }>;
export async function linkExternalIdentity(input: LinkExternalIdentityInput): Promise<void>;
```

- [x] **Step 1：备份全部将修改的现有文件**

逐个创建 `.bak-20260908-v110-question-bank`，随后运行 `git status --short --ignored` 确认备份均被忽略。

- [x] **Step 2：编写身份和兼容失败测试**

至少覆盖：

```ts
expect(normalizePersonAlias(' Ni-Ko_ ')).toBe('niko');
expect(first.personUid).not.toBe(second.personUid);
expect(first.nickname).toBe(second.nickname);
await expect(linkSameHltvIdToTwoPeople()).rejects.toMatchObject({ code: 'EXTERNAL_IDENTITY_CONFLICT' });
await expect(resolveLegacyImportTarget({ nickname: 'Alex' })).rejects.toMatchObject({ code: 'AMBIGUOUS_PLAYER_IDENTITY' });
```

补充：选手和教练 HLTV ID 可关联同一人物；按 `personUid` 导入优先；唯一昵称旧格式仍能更新；候选缺字段人物不进入旧缓存；主要昵称变更保留旧别名。

- [x] **Step 3：运行测试并确认失败**

```powershell
Set-Location 'friberg-game'
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/services/questionBank/identity.test.ts test/services/playerCache.test.ts test/routes/externalPlayers.integration.test.ts --no-file-parallelism
```

预期：缺少新服务或旧接口仍按昵称任选/冲突而失败。

- [x] **Step 4：实现人物身份服务**

- UUID 只生成一次并持久化；
- 新人物默认 `identity_status=candidate`、`is_enabled=false`；
- 不按昵称自动合并；
- 外部身份冲突写入 `identity_review_cases` 后返回明确错误；
- 主要昵称更新在同一事务中维护 `players.nickname` 和别名；
- `isLegacyGuessReady()` 验证旧比较逻辑需要的全部非空字段。

- [x] **Step 5：改造旧写入和导出**

- `playerSchema` 接受可选 `personUid`，创建时没有则生成；
- 批量导入优先按 `personUid`，昵称只在恰好一条匹配时兼容；
- `player_change_submissions` 使用相同解析规则；
- 导出固定包含 `personUid`；
- 别名和外部身份作为人物子记录随尚未被引用的候选人物删除；已发布版本、证据和题目历史的删除保护在相应表建立后追加；
- 种子逻辑只服务现有五名旧数据，不扩展为新版来源导入器。

- [x] **Step 6：保护原版缓存**

`refreshPlayerCache()` 只把 `is_enabled=true` 且 `isLegacyGuessReady()` 的人物加入原版 `allPlayers` 和旧难度池。若数据库存在启用但字段不完整人物，刷新必须抛出结构化配置错误，不能静默隐藏生产数据错误。

- [x] **Step 7：运行相关测试**

```powershell
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/services/questionBank/identity.test.ts test/services/playerMutations.identity.test.ts test/services/playerChangeSubmissions.identity.test.ts test/services/playerCache.identity.test.ts --no-file-parallelism
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest list test/services/playerCache.test.ts test/routes/externalPlayers.integration.test.ts test/routes/playerChangeSubmissions.integration.test.ts test/routes/admin.integration.test.ts
```

预期：不依赖 Redis 的身份、同名、导入导出和原版缓存测试全部通过；四个既有 Redis 集成文件成功完成 TypeScript 收集。当前环境不把“成功收集”冒充为集成测试通过，实际运行留到 Redis 7.4 CI。

- [x] **Step 8：检查 Task 2 差异并暂停**

返回工作树根目录，运行 `git diff --check`、`git status --short --ignored` 和 `git diff --stat`。核对旧 `player_difficulties` 没有被重定义。

---

### Task 3：来源证据与历史事实模型

**文件：**

- 新建：`friberg-game/server/src/db/migrations/002-question-bank-facts.ts`
- 新建：`friberg-game/server/src/services/questionBank/evidence.ts`
- 新建：`friberg-game/server/src/services/questionBank/dates.ts`
- 新建：`friberg-game/server/test/services/questionBank/evidence.test.ts`
- 修改：`friberg-game/server/src/db/migrations/index.ts`
- 修改：`friberg-game/server/src/db/ready.ts`
- 修改：`friberg-game/server/src/services/questionBank/types.ts`
- 修改：`friberg-game/server/src/services/questionBank/identity.ts`
- 修改：`friberg-game/server/test/db/migrations.test.ts`
- 修改：`friberg-game/server/test/db/ready.test.ts`

**接口：**

```ts
export type EvidenceStatus = 'candidate' | 'valid' | 'invalid' | 'conflict';
export type FindingSeverity = 'info' | 'warning' | 'blocking';

export async function createImportBatch(input: ImportBatchInput): Promise<number>;
export async function upsertEvidenceCandidate(input: EvidenceCandidateInput): Promise<number>;
export async function appendFactRevision<T extends FactKind>(input: FactRevisionInput<T>): Promise<number>;
export async function recordValidationFinding(input: ValidationFindingInput): Promise<number>;
```

- [x] **Step 1：备份迁移注册表、就绪检查、类型和迁移测试**

创建对应被忽略备份并核对状态。

- [x] **Step 2：编写证据与事实失败测试**

测试必须证明：

- 相同来源记录和内容哈希重复导入幂等；
- `evidence_date` 与 `retrieved_at` 分开保存；
- 事实纠错创建新修订并保留旧行；
- 排名一期不能有重复名次或重复战队；
- 外部战队 ID 唯一；
- 成员时间段结束日必须晚于开始日；
- 2012 可标记 `officially_not_published`；
- 个人贴纸与队标贴纸明确区分；
- `person_match_evidence` 接受 `cs16`、`csgo`、`cs2`，拒绝未知游戏版本。

- [x] **Step 3：运行失败测试**

```powershell
Set-Location 'friberg-game'
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/services/questionBank/evidence.test.ts test/db/migrations.test.ts --no-file-parallelism
```

预期：`002` 迁移和证据服务不存在而失败。

- [x] **Step 4：实现 `002-question-bank-facts`**

按设计文档创建：

- `data_import_batches`
- `source_evidence`
- `data_validation_findings`
- `teams`、`team_aliases`、`team_external_identities`
- `ranking_snapshots`、`ranking_entries`
- `team_memberships`
- `annual_top_lists`、`annual_top_entries`
- `majors`、`major_team_results`、`major_roster_members`
- `personal_stickers`
- `person_match_evidence`

可独立修订的事实表加入不可变修订 ID、`import_batch_id`、`evidence_id`、`supersedes_id` 和审核状态；榜单条目、Major 战队结果与阵容等子项随父快照修订。所有日期使用 `date`，并由服务层校验真实自然日；枚举同时受服务输入和数据库约束保护。

- [x] **Step 5：实现证据写入服务**

- 规范化 payload 后计算 SHA-256；
- 幂等键使用来源、记录键和内容哈希；
- 已发布事实不更新原行；
- 冲突生成 `data_validation_findings` 或 `identity_review_cases`；
- 不包含任何联网抓取逻辑。

- [x] **Step 6：更新就绪检查并运行测试**

```powershell
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/services/questionBank/evidence.test.ts test/db/migrations.test.ts test/db/ready.test.ts --no-file-parallelism
```

预期：证据、事实修订和双数据库通用约束的 SQLite 测试通过。

- [x] **Step 7：检查 Task 3 差异并暂停**

运行差异、安全和意外文件检查；确认没有加入真实网页内容、账号、Token 或生产配置。

---

### Task 4：不可变数据版本与纯资格计算器

**文件：**

- 新建：`friberg-game/server/src/db/migrations/003-question-bank-versions.ts`
- 新建：`friberg-game/server/src/services/questionBank/qualification.ts`
- 新建：`friberg-game/server/src/services/questionBank/materialization.ts`
- 新建：`friberg-game/server/test/services/questionBank/qualification.test.ts`
- 新建：`friberg-game/server/test/services/questionBank/materialization.test.ts`
- 修改：`friberg-game/server/src/db/migrations/index.ts`
- 修改：`friberg-game/server/src/db/ready.ts`
- 修改：`friberg-game/server/src/services/questionBank/types.ts`
- 修改：`friberg-game/server/test/db/migrations.test.ts`
- 修改：`friberg-game/server/test/db/ready.test.ts`

**接口：**

```ts
export type QuestionBankPoolKey =
  | 'top'
  | 'regular_simple'
  | 'regular_normal'
  | 'regular_hard'
  | 'regular_expert';

export interface QualificationInput {
  cutoffDate: string;
  people: readonly VersionPersonFacts[];
  externalIdentities: readonly VersionExternalIdentity[];
  matchEvidence: readonly VersionMatchEvidence[];
  rankings: readonly VersionRankingSnapshot[];
  memberships: readonly VersionTeamMembership[];
  annualTopLists: readonly VersionAnnualTopList[];
  majors: readonly VersionMajorFacts[];
  stickers: readonly VersionStickerFacts[];
}

export interface QualificationOutput {
  universe: ReadonlySet<number>;
  top: ReadonlySet<number>;
  rawSimple: ReadonlySet<number>;
  rawNormal: ReadonlySet<number>;
  rawHard: ReadonlySet<number>;
  reasons: readonly QualificationReason[];
}

export function calculateQualifications(input: QualificationInput): QualificationOutput;
export function materializeQuestionBankPools(output: QualificationOutput): Map<QuestionBankPoolKey, ReadonlySet<number>>;
export function combinePoolWithTop(pools: ReadonlyMap<QuestionBankPoolKey, ReadonlySet<number>>, poolKey: Exclude<QuestionBankPoolKey, 'top'>): ReadonlySet<number>;
export function validatePoolInvariants(input: PoolValidationInput): PoolValidationFinding[];
```

- [x] **Step 1：备份将修改的迁移、就绪和类型文件**

创建被忽略备份并核对状态。

- [x] **Step 2：编写资格计算失败测试**

使用小型合成夹具逐项测试：

```ts
expect(result.universe).toEqual(new Set([playerWithMatch, coachWithProfile]));
expect(result.universe.has(playerWithoutMatch)).toBe(false);
expect(result.top.has(repeatedTopPlayer)).toBe(true);
expect(result.top.has(listedOnlyIn2012)).toBe(false);
expect(result.rawSimple.has(vrsTop10OfficialMember)).toBe(true);
expect(result.rawSimple.has(vrsTop10LoanPlayer)).toBe(false);
expect(result.rawNormal.has(signatureStickerOwner)).toBe(true);
expect(result.rawHard.has(hltvTop30OfficialCoach)).toBe(true);
```

边界测试必须覆盖三个规则起算日、榜单下一期发布日期、成员同日加入/离开、开放结束日期和截止日期加一日。

- [x] **Step 3：编写集合物化失败测试**

断言：

```ts
expect(isSubset(simple, normal)).toBe(true);
expect(isSubset(normal, hard)).toBe(true);
expect(isSubset(hard, expert)).toBe(true);
expect(intersection(top, expert).size).toBe(0);
expect(union(top, expert)).toEqual(universe);
expect(union(simple, top)).toEqual(expectedSimpleWithTop);
```

加入 `T` 中人物缺少 `U` 证据时产生 `blocking`，以及同一人物多条资格只生成一个成员的测试。

- [x] **Step 4：运行测试并确认失败**

```powershell
Set-Location 'friberg-game'
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/services/questionBank/qualification.test.ts test/services/questionBank/materialization.test.ts --no-file-parallelism
```

- [x] **Step 5：实现纯资格计算器**

- 不访问数据库、Redis、网络、时钟或随机数；
- 所有当前日期由 `cutoffDate` 显式传入；
- 使用 `[start, end)` 自然日区间；
- 只接收 `identity_status=verified` 且未合并人物；
- `U`、`T`、`S0`、`N0`、`H0` 严格按设计公式；
- 所有结果先与 `U` 取交集；
- 永久资格由截止日前任何有效历史命中产生；
- 每个命中生成可追溯 `QualificationReason`。

- [x] **Step 6：实现版本表和物化表迁移**

创建：

- `data_versions`
- 全部 `version_*` 事实快照表
- `qualification_results`
- `qualification_evidence_links`
- `question_bank_memberships`
- `data_admin_audit_log`
- `published_dataset_pointer`
- `data_validation_findings.version_id`

成员主键固定为 `(version_id, pool_key, player_id)`。已经发布版本的内容写入由服务层拒绝，并通过数据库权限/触发器可行范围内增加保护；SQLite 测试至少验证服务层不可变性。

- [x] **Step 7：实现集合物化与阻断校验**

物化五个基础集合，不物化“包含 Top”组合。生成设计文档第 9.8 节所有不变量检查，并把失败转成结构化 finding。

- [x] **Step 8：运行 Task 4 测试**

```powershell
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/services/questionBank/qualification.test.ts test/services/questionBank/materialization.test.ts test/db/migrations.test.ts test/db/ready.test.ts --no-file-parallelism
```

预期：所有纯规则、物化和迁移测试通过。

- [x] **Step 9：检查 Task 4 差异并暂停**

核对算法中没有 `beginner` 或旧 `easy`，也没有把 `player_difficulties` 当新版资格来源。

---

### Task 5：版本构建、变化报告、审核、发布与回滚

**文件：**

- 修改：`friberg-game/server/src/db/migrations/002-question-bank-facts.ts`
- 修改：`friberg-game/server/src/db/migrations/003-question-bank-versions.ts`
- 修改：`friberg-game/server/src/db/ready.ts`
- 修改：`friberg-game/server/src/services/questionBank/evidence.ts`
- 新建：`friberg-game/server/src/services/questionBank/versionBuilder.ts`
- 新建：`friberg-game/server/src/services/questionBank/publication.ts`
- 新建：`friberg-game/server/test/services/questionBank/versionBuilder.test.ts`
- 新建：`friberg-game/server/test/services/questionBank/publication.test.ts`
- 修改：`friberg-game/server/src/services/questionBank/types.ts`
- 修改：`friberg-game/server/test/db/migrations.test.ts`
- 修改：`friberg-game/server/test/services/questionBank/evidence.test.ts`

**接口：**

```ts
export async function createDataVersion(input: {
  versionUid?: string;
  displayVersion: string;
  cutoffDate: string;
  basedOnVersionUid?: string;
  actorUserId: number;
}): Promise<{ versionId: number; versionUid: string }>;

export async function buildDataVersion(input: {
  versionUid: string;
  actorUserId: number;
}): Promise<VersionBuildReport>;

export async function submitDataVersionForReview(versionUid: string, actorUserId: number): Promise<void>;

export async function publishDataVersion(input: {
  versionUid: string;
  expectedCurrentVersionUid: string | null;
  reason: string;
  idempotencyKey: string;
  actorUserId: number;
}): Promise<PublicationResult>;

export async function rollbackDataVersion(input: {
  targetVersionUid: string;
  expectedCurrentVersionUid: string;
  reason: string;
  idempotencyKey: string;
  actorUserId: number;
}): Promise<PublicationResult>;
```

- [x] **Step 1：编写版本构建失败测试**

测试：

- 从上一版本复制选定事实并应用已审核新修订；
- 截止日期后的事实不进入快照；
- 当前战队、身份和状态按截止日生成；
- 同日存在冲突事实时产生 finding，不任选一条；
- 排序规范化内容得到稳定 SHA-256；
- 相比上一版本生成新增、移除、资格变化和缺字段统计；
- 构建不切换正式指针。

- [x] **Step 2：编写发布失败测试**

测试：

```ts
await expect(publishDraftWithBlockingFindings()).rejects.toMatchObject({ code: 'VERSION_HAS_BLOCKERS' });
await expect(publishWithStaleExpectedVersion()).rejects.toMatchObject({ code: 'PUBLISHED_VERSION_CHANGED' });
expect(await currentVersion()).toBe(versionUid);
expect(await versionRowsAreUnchangedAfterPublish()).toBe(true);
expect(await repeatSameIdempotencyKey()).toEqual(firstResult);
expect(await rollbackToPublishedVersion()).toMatchObject({ currentVersionUid: oldVersionUid });
```

- [x] **Step 3：运行失败测试**

```powershell
Set-Location 'friberg-game'
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/services/questionBank/versionBuilder.test.ts test/services/questionBank/publication.test.ts --no-file-parallelism
```

- [x] **Step 4：实现版本构建器**

先创建包含显示版本号、截止日期和基础版本的草稿，再单独构建。在单一事务中复制版本事实快照、调用纯资格计算器、写资格原因、物化成员、写 findings、计算内容哈希和变化报告。构建失败不留下半成品快照；重复构建同一未发布版本先清除该草稿自己的派生行，不接触已发布版本。人物基础资料来自新增的 `person_profile_facts`；战队名称来自截止日有效且证据完整的主要 `team_aliases`，不得复制旧可变显示字段作为正式事实。

- [x] **Step 5：实现审核、发布与回滚**

- 状态机只允许 `draft → validating → in_review → published`；
- 只有 `in_review` 且无 blocker 的版本可发布；
- 使用数据库行锁和 `expectedCurrentVersionUid` 防止并发覆盖；
- 指针和版本状态在同一事务更新；
- 审计日志与操作结果在同一事务写入；
- Redis 通知不在事务内，本任务只产生提交后通知钩子；
- 回滚目标必须是已有 `published` 版本。

- [x] **Step 6：运行 Task 5 测试**

```powershell
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/services/questionBank/versionBuilder.test.ts test/services/questionBank/publication.test.ts test/services/questionBank/qualification.test.ts test/services/questionBank/materialization.test.ts --no-file-parallelism
```

预期：版本内容不可变、发布原子、并发保护和审计测试通过。

- [x] **Step 7：检查 Task 5 差异并暂停**

确认没有任何测试或初始化代码自动创建正式版本，也没有真实发布动作。

---

### Task 6：版本化读取、缓存与公开题库目录

**文件：**

- 新建：`friberg-game/server/src/services/questionBank/repository.ts`
- 新建：`friberg-game/server/src/services/questionBank/cache.ts`
- 新建：`friberg-game/server/src/services/questionBank/catalog.ts`
- 新建：`friberg-game/server/src/routes/questionBank.ts`
- 新建：`friberg-game/server/test/services/questionBank/cache.test.ts`
- 新建：`friberg-game/server/test/services/questionBank/repository.test.ts`
- 新建：`friberg-game/server/test/routes/questionBank.integration.test.ts`
- 修改：`friberg-game/server/src/index.ts`
- 修改：`friberg-game/server/src/services/questionBank/types.ts`
- 检查但无需修改：`friberg-game/server/src/redis.ts`

**接口：**

```ts
export interface ResolveQuestionBankPoolInput {
  versionUid: string;
  bank: 'top' | 'regular';
  difficulty: 'simple' | 'normal' | 'hard' | 'expert' | null;
  includeTop: boolean;
}

export async function resolveQuestionBankPool(
  input: ResolveQuestionBankPoolInput
): Promise<ResolvedQuestionBankPool>;

export async function getPublishedQuestionBankCatalog(): Promise<QuestionBankCatalog>;
export async function notifyQuestionBankPublished(versionUid: string): Promise<void>;
```

- [x] **Step 1：备份 `index.ts` 和 `redis.ts`**

创建被忽略备份并核对状态。

- [x] **Step 2：编写缓存失败测试**

通过注入假的数据库仓库和 Redis 适配器测试：

- 缓存键包含 `version_uid`；
- 五个基础集合可独立加载；
- “包含 Top”使用去重并集；
- 发布新版本不删除旧版本缓存；
- Redis 缺失时从数据库加载；
- 通知丢失时读取正式指针可发现新版本；
- 数据库无正式版本返回 `QUESTION_BANK_VERSION_UNAVAILABLE`，不是空集合。

- [x] **Step 3：编写公开路由失败测试**

断言 `/api/question-bank/catalog`：

- 只返回正式版本、截止日期、组合人数和可开始状态；
- 不返回人物列表、证据、正确答案、草稿或审核人；
- 无正式版本时返回明确不可用状态；
- `top`、普通四档、包含/不包含 Top 的人数正确。

- [x] **Step 4：运行失败测试**

```powershell
Set-Location 'friberg-game'
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/services/questionBank/cache.test.ts test/routes/questionBank.integration.test.ts --no-file-parallelism
```

- [x] **Step 5：实现仓库、缓存和统一解析服务**

- 数据库仓库按 `versionUid` 读取不可变快照和五个成员集合；
- 进程缓存使用有容量上限的 LRU 或等价有界结构；
- Redis 只作为可选加速和 Pub/Sub；
- 每次新开局前的领域调用核对数据库正式指针；
- `top` 拒绝难度和 `includeTop=true`；
- 普通题库必须指定难度；
- 返回成员数与截止日期；
- 人数表示人物数，不表示题目模板容量。
- 按版本缓存题目安全的人物快照及版本化当前战队名称；同一版本/集合的并发首次加载必须合并。

- [x] **Step 6：挂载公开路由**

在 `index.ts` 将 `/api/question-bank` 挂载在现有 PoW 与 JSON 边界内，不改变其他路由顺序和安全中间件。公开目录不接受客户端指定草稿版本。

- [x] **Step 7：运行 Task 6 测试**

```powershell
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/services/questionBank/cache.test.ts test/services/questionBank/repository.test.ts test/routes/questionBank.integration.test.ts test/services/resourceVersion.test.ts test/services/playerCache.identity.test.ts --no-file-parallelism
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest list test/services/playerCache.test.ts
```

预期：题库缓存、仓库、公开路由、现有资源版本和无 Redis 人物缓存回归通过；既有 Redis 人物缓存测试成功完成收集，实际运行留待 Redis 7.4 CI。

- [x] **Step 8：检查 Task 6 差异并暂停**

确认没有复用 `resource:version`，没有在公开响应泄露人物事实或证据。

---

### Task 7：管理审核 API 与外部候选批次 API

**文件：**

- 新建：`friberg-game/server/src/routes/adminQuestionBank.ts`
- 新建：`friberg-game/server/src/routes/externalQuestionBank.ts`
- 新建：`friberg-game/server/test/routes/adminQuestionBank.integration.test.ts`
- 新建：`friberg-game/server/test/routes/externalQuestionBank.integration.test.ts`
- 修改：`friberg-game/server/src/index.ts`
- 修改：`friberg-game/server/src/services/questionBank/evidence.ts`
- 修改：`friberg-game/server/src/services/questionBank/publication.ts`

**接口：**

管理路由固定前缀：`/api/admin/question-bank`。

```text
GET  /versions
GET  /versions/:versionUid
POST /versions
POST /versions/:versionUid/build
POST /versions/:versionUid/submit-review
POST /versions/:versionUid/publish
POST /versions/:versionUid/rollback
GET  /versions/:versionUid/change-report
GET  /people/:personUid/qualifications
GET  /validation-findings
POST /identity-review-cases/:id/resolve
```

外部候选入口固定为：

```text
POST /api/external/question-bank/import-batches
```

- [x] **Step 1：备份将再次修改的现有文件**

如果 Task 6 已创建备份，不覆盖原始备份；为本任务再创建带 `-task7` 后缀的备份。

- [x] **Step 2：编写管理 API 失败测试**

覆盖：

- 普通用户不能读取管理数据；
- 管理员能查看版本、变化报告、资格解释和 findings；
- 发布必须提供原因、幂等键和预期当前版本；
- blocker、错误状态和并发旧指针均拒绝发布；
- 回滚目标必须已发布；
- 身份复核结果写审计日志；
- 管理响应不返回原始秘密配置或 Token。

- [x] **Step 3：编写外部 API 失败测试**

覆盖：

- 无 Token、失效 Token、超限请求被拒绝；
- 有效 Token 能提交候选批次；
- 相同幂等键或来源哈希不会重复创建；
- 外部 Token 不能调用审核、发布或回滚；
- 提交候选不会改变正式版本指针。

- [x] **Step 4：运行失败测试**

```powershell
Set-Location 'friberg-game'
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/routes/adminQuestionBank.integration.test.ts test/routes/externalQuestionBank.integration.test.ts --no-file-parallelism
```

- [x] **Step 5：实现 Zod 请求结构和管理路由**

- 所有版本 UID、日期、理由和幂等键有长度及格式限制；
- 管理读写沿用现有 `requireAuth`、`requireAdmin` 和速率限制；
- 路由只调用服务层，不复制资格或发布逻辑；
- 发布事务成功后调用 `notifyQuestionBankPublished()`；通知失败返回发布成功并记录告警，不能谎称数据库回滚。

- [x] **Step 6：实现外部候选入口**

- 沿用现有 `externalPlayerAuth` 和独立写入速率限制；
- 请求大小使用现有管理导入上限；
- 只创建 `data_import_batches`、候选证据和事实修订；
- 返回批次 ID、接受数量、重复数量和 validation finding 数量。

- [x] **Step 7：运行 Task 7 测试**

```powershell
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/routes/adminQuestionBank.integration.test.ts test/routes/externalQuestionBank.integration.test.ts test/routes/externalPlayers.integration.test.ts test/routes/admin.integration.test.ts --no-file-parallelism
```

预期：权限、幂等、发布边界和旧接口回归通过。

实际：`vitest list` 成功收集新增 5 个用例，TypeScript 构建通过；本机 Vitest worker 在运行阶段异常退出，且对既有 `admin.integration.test.ts` 也可复现，因此运行通过状态留待 Node 26 环境复核，不能据此声称测试通过。

- [x] **Step 8：检查 Task 7 差异并暂停**

核对 `admin.ts` 没有继续膨胀，管理题库路由保持独立；核对外部 Token 没有发布权限。

---

### Task 8：完整回归、双数据库边界与文档收尾

**文件：**

- 修改：`.github/workflows/friberg-game.yml`
- 修改：`friberg-game/UPSTREAM.md`
- 修改：`docs/2026-09-08-v1.10-player-data-question-bank-technical-design.md`（仅更新实施状态和实际偏差）
- 按验证结果修改直接相关测试或实现文件，不做无关重构。

- [x] **Step 1：备份 CI 工作流和两个文档**

创建被忽略备份；不覆盖此前任何备份。CI 工作流备份也必须匹配根 `.gitignore` 的备份规则。

- [x] **Step 2：运行全部不依赖 Redis 的题库与迁移测试**

```powershell
Set-Location 'friberg-game'
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/db/migrations.test.ts test/services/questionBank test/routes/questionBank.integration.test.ts test/routes/adminQuestionBank.integration.test.ts test/routes/externalQuestionBank.integration.test.ts --no-file-parallelism
```

预期：全部通过。若某测试实际依赖 Redis，先检查是否能通过依赖注入改成确定性单元测试；确实属于跨实例集成的部分记录为待 Redis 7.4 环境验证。

- [x] **Step 3：运行原版直接相关回归**

```powershell
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec vitest run test/db test/services/playerCache.test.ts test/services/targetSelection.test.ts test/routes/externalPlayers.integration.test.ts test/routes/game.integration.test.ts test/routes/dailyChallenge.integration.test.ts --no-file-parallelism
```

预期：不需要 Redis 的用例通过；Redis 集成用例如因 `127.0.0.1:6379` 缺失失败，保留准确数量和错误，不把它记为功能失败或通过。

- [x] **Step 4：验证服务端和客户端构建**

```powershell
& '..\node_modules\portable-bin\pnpm.cmd' exec node --version
& '..\node_modules\portable-bin\pnpm.cmd' --filter server exec node --version
& '..\node_modules\portable-bin\pnpm.cmd' build
```

预期：两个 Node 版本均为 v26.x；服务端 TypeScript 和客户端构建成功。保留已知 Vite 动静态导入警告，但新增警告必须调查。

- [x] **Step 5：增加 PostgreSQL 迁移 CI 边界**

修改 `.github/workflows/friberg-game.yml`，在现有 Redis 7.4 服务旁增加 PostgreSQL 服务：

```yaml
postgres:
  image: postgres:16-alpine
  env:
    POSTGRES_USER: friberg_test
    POSTGRES_PASSWORD: friberg_test
    POSTGRES_DB: friberg_test
  ports:
    - 5432:5432
  options: >-
    --health-cmd "pg_isready -U friberg_test -d friberg_test"
    --health-interval 5s
    --health-timeout 3s
    --health-retries 10
```

新增 PostgreSQL 专用测试步骤：

```yaml
- name: Test PostgreSQL migrations and question bank
  env:
    TEST_DATABASE_URL: postgresql://friberg_test:friberg_test@127.0.0.1:5432/friberg_test
  run: pnpm --filter server exec vitest run test/db/migrations.postgres.test.ts test/services/questionBank/postgres.integration.test.ts --no-file-parallelism
```

相应新增：

- `friberg-game/server/test/db/migrations.postgres.test.ts`
- `friberg-game/server/test/services/questionBank/postgres.integration.test.ts`

两个测试读取 `TEST_DATABASE_URL`，使用自身创建的独立测试 schema，并在 `afterAll` 删除该 schema；变量缺失时明确跳过 PostgreSQL 专用套件。当前本机不启动 PostgreSQL 或外部应用；未经用户授权不推送，因此本阶段只能检查 YAML 和测试代码，不能声称 CI 已运行。

- [x] **Step 6：检查原版集合迁移前后相等**

使用迁移测试夹具或数据库备份副本导出以下摘要并逐项比较：

```text
players.id + nickname
player_difficulties(player_id, difficulty_key)
games.target_player_id
daily_challenges.target_player_id
原版 beginner/easy/normal 缓存成员 ID
```

预期：除新增 UID、别名和允许同名所需结构外，旧数据和旧集合完全一致。

- [x] **Step 7：更新 AGPL 修改记录和技术设计状态**

`friberg-game/UPSTREAM.md` 只记录实际完成的草人杯阶段和修改范围，不声称选择题玩法已完成。技术设计文档列出实施完成项、未验证的 PostgreSQL/Redis 边界及任何已批准偏差。

- [x] **Step 8：最终安全检查**

```powershell
Set-Location '..'
git diff --check
git status --short --branch
git status --short --ignored
git diff --stat
git diff --name-only
```

必须确认：

- 没有数据库、ZIP、日志、`.env`、构建产物或备份进入可提交列表；
- 没有修改工作树外文件；
- 没有 ParticleMenu、Diagnostics 或其他组件改动；
- 没有提交、推送、PR、Release 或部署；
- 所有无法执行的验证都有准确原因和范围。

检查点：向用户汇报实际通过的测试、待外部环境验证项、差异范围和下一步选择。未经明确授权不执行 Git 提交。

Task 8 实际结果：规则与缓存单元测试 14 项通过；服务端和客户端构建通过，Vite 仅报告既有动态/静态导入警告。迁移及路由数据库测试在本机 Node 24/当前 better-sqlite3 worker 环境中异常退出，未记为通过。PostgreSQL 专用测试和 Redis 7.4 集成测试已加入 CI 配置但尚未运行。迁移夹具继续核对 `players.id`、`games`、`daily_challenges`、难度成员和同名 UID 保持关系不变。

---

## 实施完成判定

只有同时满足以下条件，才能把本计划标记为实施完成：

1. 三组迁移能从旧 SQLite 结构升级并保持旧 ID、外键和难度成员；
2. 人物 UID、同名、别名和 HLTV 选手/教练身份约束通过测试；
3. 历史事实和证据能生成不可变版本快照；
4. `U`、`T`、四档普通集合及“包含 Top”算法通过边界测试；
5. blocker 能阻止发布，发布/回滚具备事务、并发和审计保护；
6. 缓存按版本隔离，Redis 故障不改变数据库事实；
7. 玩家、管理和外部 Token API 权限边界通过测试；
8. 原版猜选手数据、缓存和直接相关测试没有回归；
9. PostgreSQL/Redis 7.4 未验证部分被明确标注，不能用 SQLite 单元测试替代声称通过；
10. 用户尚未授权的提交、推送、部署和正式数据发布均未发生。

本计划完成后仍不代表选择题玩法完成。下一阶段需要单独设计并实现第一批人物事实字段映射、题目模板、选择题状态机和前端交互。
