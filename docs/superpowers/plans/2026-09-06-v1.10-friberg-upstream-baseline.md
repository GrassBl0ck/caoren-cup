# 草人杯 1.10 弗一把上游基线 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. 根据项目 `AGENTS.md`，默认由主代理逐项执行；未经用户明确同意不得创建子代理。Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `shnlfriberg/csgofriberg` 的固定提交完整、可追溯地引入草人杯 `friberg-game/`，保留 AGPL-3.0 边界，并建立不改变上游玩法的测试基线。

**Architecture:** `friberg-game/` 是草人杯仓库中的独立顶层组件，保留上游 React、Express、PostgreSQL、Redis 与 pnpm workspace 结构。本阶段只做源码快照、许可证说明和基线验证，不连接 `web-command-center/`、草人杯账号、Release 或生产部署。

**Tech Stack:** Node.js 26、pnpm 11.13.1、React 18、Vite 6、TypeScript 5.7、Express 4、Socket.IO 4、PostgreSQL、Redis、Vitest。

## Global Constraints

- 工作树固定为 `D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg`，分支固定为 `feat/v1.10-friberg`。
- 上游仓库固定为 `https://github.com/shnlfriberg/csgofriberg`。
- 上游快照固定为提交 `ad8439894c14a50fd1a40294e46942aa530c99cf`，提交时间 `2026-08-26T05:28:08Z`。
- `friberg-game/` 及其草人杯修改继续使用 AGPL-3.0，不适用仓库根 MIT 许可证。
- 所有文本文件使用 UTF-8。
- 不覆盖 `web-command-center/`、`game-plugin/`、`mini-games-plugin/`、`desktop-client/` 或异能模式工作树。
- 暂停开发的 `game-plugin/Features/ParticleMenu/` 与 `game-plugin.Tests/ParticleMenu/` 不处理。
- 私有 `game-plugin/Diagnostics/` 与 `game-plugin.Tests/Diagnostics/` 不处理。
- 本阶段不导入第三方 646 人镜像，不修改上游题库，不实现选择题玩法。
- 本阶段不修改 Release 脚本、部署配置或服务器文件，不启动生产服务。
- 修改现有文件前必须创建 `.bak-20260906-friberg-baseline` 备份，并确认备份被 Git 忽略。
- 未经用户明确授权，不执行 `git add`、`git commit`、`git push` 或创建 PR。
- 当前本机 PATH 为 Node.js 22，Codex bundled Node 为 24，且没有 Docker；Node.js 26 验证是显式门槛，不能用 Node 22/24 的结果冒充 Node 26 通过。

---

## Scope decomposition

本计划只实施“阶段 0：上游基线”。以下内容必须在阶段 0 验收后分别编写后续计划：

1. 阶段 1：同域名入口、子路径与草人杯导航集成；
2. 阶段 2：人物身份、别名、证据、榜单快照和题库集合数据模型；
3. 阶段 3：第一批实时选择题生成器与单人模式；
4. 阶段 4：1v1 房间、生命值、计时、断线和观战；
5. 阶段 5：第二、第三批题型；
6. 阶段 6：账号、统计、Release、部署与生产验收。

阶段 0 不提前创建后续阶段的占位代码。

---

### Task 1: 安全导入固定上游源码快照

**Files:**
- Create: `friberg-game/**`（来自固定提交的完整上游文件树）
- Do not modify: `web-command-center/**`
- Do not modify: `game-plugin/**`

**Interfaces:**
- Consumes: GitHub 固定提交归档 `ad8439894c14a50fd1a40294e46942aa530c99cf`
- Produces: 独立且不包含嵌套 `.git/` 的 `friberg-game/` 上游源码树

- [ ] **Step 1: 检查工作树、目标和临时目录**

Run:

```powershell
$repo = 'D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg'
$target = Join-Path $repo 'friberg-game'
$stage = Join-Path $repo '.vendor-staging\csgofriberg-ad84398'
git -C $repo status --short --branch
git -C $repo rev-parse --show-toplevel
Test-Path -LiteralPath $target
Test-Path -LiteralPath $stage
```

Expected:

- 当前分支为 `feat/v1.10-friberg`；
- Git 根目录等于 `$repo`；
- `$target` 和 `$stage` 均返回 `False`；
- 状态中只允许存在两份已确认 PRD 和本实施计划，不得出现来源不明的 `friberg-game/`。

- [ ] **Step 2: 创建仓库内临时导入目录并下载固定提交 ZIP**

Run:

```powershell
$repo = 'D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg'
$stageRoot = Join-Path $repo '.vendor-staging'
$archive = Join-Path $stageRoot 'csgofriberg-ad8439894c14a50fd1a40294e46942aa530c99cf.zip'
New-Item -ItemType Directory -Path $stageRoot -ErrorAction Stop | Out-Null
Invoke-WebRequest `
  -Uri 'https://github.com/shnlfriberg/csgofriberg/archive/ad8439894c14a50fd1a40294e46942aa530c99cf.zip' `
  -OutFile $archive `
  -ErrorAction Stop
Get-Item -LiteralPath $archive | Select-Object FullName, Length
```

Expected: ZIP 存在且长度大于 100 KB。若下载失败，停止，不改用浮动 `main` 或第三方镜像。

- [ ] **Step 3: 解压并验证上游身份**

Run:

```powershell
$repo = 'D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg'
$stageRoot = Join-Path $repo '.vendor-staging'
$archive = Join-Path $stageRoot 'csgofriberg-ad8439894c14a50fd1a40294e46942aa530c99cf.zip'
Expand-Archive -LiteralPath $archive -DestinationPath $stageRoot -ErrorAction Stop
$source = Join-Path $stageRoot 'csgofriberg-ad8439894c14a50fd1a40294e46942aa530c99cf'
$files = @(Get-ChildItem -LiteralPath $source -Recurse -File)
[pscustomobject]@{
    SourceExists = Test-Path -LiteralPath $source
    FileCount = $files.Count
    HasLicense = Test-Path -LiteralPath (Join-Path $source 'LICENSE')
    HasRootPackage = Test-Path -LiteralPath (Join-Path $source 'package.json')
    HasClientPackage = Test-Path -LiteralPath (Join-Path $source 'client\package.json')
    HasServerPackage = Test-Path -LiteralPath (Join-Path $source 'server\package.json')
    HasNestedGit = Test-Path -LiteralPath (Join-Path $source '.git')
} | Format-List
```

Expected:

- `SourceExists=True`；
- 上游 API 盘点基线为 287 个 blob，归档文件数应与其一致；如 GitHub 归档额外生成文件导致数量不同，必须列出差异并停止导入；
- 三个 `package.json` 和 `LICENSE` 存在；
- `HasNestedGit=False`。

- [ ] **Step 4: 将已验证文件树机械移动到独立组件目录**

在移动前再次解析绝对路径，确认 `$source` 和 `$target` 都位于当前工作树内：

```powershell
$repo = (Resolve-Path -LiteralPath 'D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg').Path
$stageRoot = Join-Path $repo '.vendor-staging'
$source = Join-Path $stageRoot 'csgofriberg-ad8439894c14a50fd1a40294e46942aa530c99cf'
$target = Join-Path $repo 'friberg-game'
if (-not $source.StartsWith($repo, [StringComparison]::OrdinalIgnoreCase)) { throw '上游解压路径越界' }
if (-not $target.StartsWith($repo, [StringComparison]::OrdinalIgnoreCase)) { throw '目标路径越界' }
if (Test-Path -LiteralPath $target) { throw '目标目录已存在，禁止覆盖' }
Move-Item -LiteralPath $source -Destination $target -ErrorAction Stop
```

Expected: `friberg-game/package.json` 存在，且 `friberg-game/.git` 不存在。

- [ ] **Step 5: 清理本次明确创建的临时归档**

清理前只允许目标为当前工作树内的 `.vendor-staging`：

```powershell
$repo = (Resolve-Path -LiteralPath 'D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg').Path
$stageRoot = Join-Path $repo '.vendor-staging'
if (-not $stageRoot.StartsWith($repo, [StringComparison]::OrdinalIgnoreCase)) { throw '临时目录越界' }
if ((Split-Path -Leaf $stageRoot) -ne '.vendor-staging') { throw '临时目录名称异常' }
Remove-Item -LiteralPath $stageRoot -Recurse -Force
Test-Path -LiteralPath $stageRoot
```

Expected: 返回 `False`。只删除本次创建的 `.vendor-staging`，不得使用通配符，不得删除 `friberg-game/`。

- [ ] **Step 6: 检查机械导入结果**

Run:

```powershell
$repo = 'D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg'
$target = Join-Path $repo 'friberg-game'
@(Get-ChildItem -LiteralPath $target -Recurse -File).Count
Get-Content -Encoding UTF8 -TotalCount 12 (Join-Path $target 'package.json')
Get-Content -Encoding UTF8 -TotalCount 8 (Join-Path $target 'LICENSE')
git -C $repo status --short --untracked-files=all -- friberg-game
```

Expected: 文件树完整、根包名为 `csgofriberg`、许可证为 GNU Affero General Public License，并且没有 ZIP、日志、构建产物或嵌套仓库。

---

### Task 2: 记录上游来源与许可证边界

**Files:**
- Create: `friberg-game/UPSTREAM.md`
- Modify: `README.md`（组件清单、项目结构和许可证章节）
- Test: manual source and license contract check

**Interfaces:**
- Consumes: Task 1 的完整上游快照
- Produces: 可审计的固定提交、导入日期、许可证与草人杯修改边界

- [ ] **Step 1: 修改 README 前创建忽略备份**

Run:

```powershell
$repo = 'D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg'
$readme = Join-Path $repo 'README.md'
$backup = "$readme.bak-20260906-friberg-baseline"
if (Test-Path -LiteralPath $backup) { throw 'README 备份已存在，禁止覆盖' }
Copy-Item -LiteralPath $readme -Destination $backup -ErrorAction Stop
git -C $repo check-ignore 'README.md.bak-20260906-friberg-baseline'
```

Expected: `git check-ignore` 输出备份路径。

- [ ] **Step 2: 创建 UPSTREAM.md**

Create `friberg-game/UPSTREAM.md` with:

```markdown
# Upstream source and modifications

## Upstream

- Project: `shnlfriberg/csgofriberg`
- Repository: <https://github.com/shnlfriberg/csgofriberg>
- Pinned commit: `ad8439894c14a50fd1a40294e46942aa530c99cf`
- Upstream commit date: `2026-08-26T05:28:08Z`
- Snapshot date: `2026-09-06`
- License: GNU Affero General Public License v3.0

The complete upstream AGPL-3.0 license is preserved as `LICENSE` in this directory.

## Snapshot policy

This directory was imported as a source snapshot, not as a Git submodule or subtree. Do not update it from a floating branch. Every upstream refresh must pin a full commit hash, review the diff, update this file, and rerun the component tests and build.

## Caoren Cup changes

No functional changes have been applied in the initial snapshot. Future Caoren Cup changes must be documented here by phase and remain available under AGPL-3.0.
```

- [ ] **Step 3: 在根 README 增加独立组件与 AGPL 例外**

Use `apply_patch` to perform these three exact documentation changes:

1. Change `项目包含四部分：` to `项目包含五部分：` and append:

```markdown
5. **弗一把选手猜测游戏（1.10 开发中）**
```

2. Add this top-level component to the project tree after `mini-games-plugin/`:

```text
├─ friberg-game/
│  └─ 独立 AGPL-3.0 选手猜测游戏（1.10 开发中）
│
```

3. Insert this paragraph after the existing weaponpaints license exception:

```markdown
许可证例外：`friberg-game/` 基于 `shnlfriberg/csgofriberg` 的固定源码快照改造，该目录及其修改使用 GNU Affero General Public License v3.0（AGPL-3.0），不适用仓库根 MIT 许可证。上游来源、固定提交和改动说明见 `friberg-game/UPSTREAM.md`。通过网络向用户提供修改版服务时，必须按 AGPL-3.0 向相应用户提供对应源码获取方式。
```

- [ ] **Step 4: 验证许可证说明**

Run:

```powershell
$repo = 'D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg'
rg -n "friberg-game|ad8439894c14a50fd1a40294e46942aa530c99cf|AGPL-3.0|GNU Affero" `
  (Join-Path $repo 'README.md') `
  (Join-Path $repo 'friberg-game\UPSTREAM.md') `
  (Join-Path $repo 'friberg-game\LICENSE')
```

Expected: 根 README、UPSTREAM 和上游 LICENSE 均能定位，且没有把 `friberg-game/` 描述为 MIT 或 GPL-3.0-only。

---

### Task 3: 建立独立 Node 26 CI 基线

**Files:**
- Create: `.github/workflows/friberg-game.yml`
- Do not modify: upstream files under `friberg-game/`
- Test: GitHub Actions syntax review and local command parity check

**Interfaces:**
- Consumes: `friberg-game/package.json` 中的 `pnpm test` 与 `pnpm build`
- Produces: 在 Node 26 环境中执行冻结安装、测试和构建的独立工作流

- [ ] **Step 1: 创建工作流**

Create `.github/workflows/friberg-game.yml` with:

```yaml
name: Friberg game

on:
  pull_request:
    paths:
      - "friberg-game/**"
      - ".github/workflows/friberg-game.yml"
  push:
    branches:
      - main
      - "feat/v1.10-friberg"
    paths:
      - "friberg-game/**"
      - ".github/workflows/friberg-game.yml"

permissions:
  contents: read

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    services:
      redis:
        image: redis:7.4-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    defaults:
      run:
        working-directory: friberg-game
    steps:
      - uses: actions/checkout@v5
      - uses: pnpm/action-setup@v5
        with:
          version: 11.13.1
          run_install: false
      - uses: actions/setup-node@v5
        with:
          node-version: 26
          cache: pnpm
          cache-dependency-path: friberg-game/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm test
      - run: pnpm build
```

- [ ] **Step 2: 验证工作流只覆盖独立组件**

Run:

```powershell
$workflow = 'D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg\.github\workflows\friberg-game.yml'
rg -n "redis:7.4-alpine|redis-cli ping|working-directory: friberg-game|node-version: 26|pnpm test|pnpm build|contents: read" $workflow
rg -n "web-command-center|game-plugin|deployment|release" $workflow
```

Expected: 第一条命令命中全部基线字段；第二条命令无输出，确认工作流不触碰其他组件、部署或发布。

- [ ] **Step 3: 记录本地运行环境限制**

Run:

```powershell
node --version
pnpm --version
docker --version
```

Expected in the current environment: Node.js `v22.13.1`、pnpm `11.19.0`，Docker 命令不可用。将此事实记录在基线报告，不能声称本地已完成 Node 26 验收。

- [ ] **Step 4: 在可用 Node 26 环境执行上游基线**

Run from `friberg-game/`:

```powershell
node --version
pnpm --version
pnpm install --frozen-lockfile
pnpm test
pnpm build
```

Expected:

- Node 输出 `v26.x.x`；
- pnpm 输出 `11.13.1`；
- 冻结安装退出码 0；
- 前后端全部测试退出码 0；
- PoW WASM、前端和后端构建退出码 0。

如果当前会话没有 Node 26，停止在此门槛并请求用户选择“允许在工作树内下载便携 Node 26”或“推送后由 GitHub Actions 验证”；不得修改系统 Node，也不得以 Node 22/24 的结果替代。

---

### Task 4: 记录基线并检查意外文件

**Files:**
- Create: `docs/2026-09-07-v1.10-friberg-upstream-baseline.md`
- Inspect: complete working tree diff

**Interfaces:**
- Consumes: Tasks 1–3 的实际命令输出
- Produces: 后续阶段可引用的上游基线验收记录

- [ ] **Step 1: 以当前已知事实创建基线报告**

Create `docs/2026-09-07-v1.10-friberg-upstream-baseline.md` with actual values only:

```markdown
# 草人杯 1.10 弗一把上游基线

## 固定来源

- 上游：`https://github.com/shnlfriberg/csgofriberg`
- 提交：`ad8439894c14a50fd1a40294e46942aa530c99cf`
- 快照日期：`2026-09-06`
- 许可证：AGPL-3.0

## 导入范围

- 导入文件数：287
- 本地功能改动：无
- 草人杯集成改动：仅许可证说明与独立 CI

## 当前本地验证环境

- Node.js：v22.13.1
- Codex bundled Node.js：v24.19.0
- pnpm：11.19.0
- Docker：不可用
- Node.js 26：当前不可用

## Node.js 26 基线验证

- `pnpm install --frozen-lockfile`：未运行（当前本地没有 Node.js 26）
- `pnpm test`：未运行（当前本地没有 Node.js 26）
- `pnpm build`：未运行（当前本地没有 Node.js 26）
- GitHub Actions：未运行（工作流尚未提交或推送）

## 已知限制

- 尚未接入草人杯导航、账号、Release 或部署。
- 尚未修改题库或实现选择题模式。
- 未完成的验证必须明确保留，不能写成通过。
```

After a real Node.js 26 run or GitHub Actions run, use `apply_patch` to replace only the four `未运行` result lines with the exact command result, test counts, build result, and Actions run URL. Do not change them to `通过` without fresh evidence.

- [ ] **Step 2: 检查源码树和禁止提交内容**

Run:

```powershell
$repo = 'D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg'
git -C $repo status --short --ignored
rg --files (Join-Path $repo 'friberg-game') `
  -g '*.zip' -g '*.rar' -g '*.7z' -g '*.log' -g '.env' -g 'node_modules/**' -g 'dist/**' -g 'coverage/**'
Get-ChildItem -LiteralPath (Join-Path $repo 'friberg-game') -Directory -Recurse -Force |
  Where-Object Name -eq '.git' |
  Select-Object -ExpandProperty FullName
```

Expected:

- `node_modules/`、`dist/` 和备份只以 ignored 状态出现；
- `friberg-game/` 跟踪候选中没有压缩包、日志、`.env`、构建产物或嵌套 `.git/`；
- 已存在的其他工作区内容不被删除或移动。

- [ ] **Step 3: 检查实际差异**

Run:

```powershell
$repo = 'D:\OpenSourcework\02-worktrees\caoren-cup\feat-v1.10-friberg'
git -C $repo diff -- README.md .github/workflows/friberg-game.yml
git -C $repo status --short --untracked-files=all -- `
  friberg-game `
  .github/workflows/friberg-game.yml `
  README.md `
  docs/2026-09-07-v1.10-friberg-upstream-baseline.md `
  docs/2026-09-06-v1.10-player-question-bank-prd.md `
  docs/2026-09-06-v1.10-choice-quiz-mode-prd.md
```

Expected: 差异仅包括固定上游快照、上游说明、根许可证说明、独立 CI、两份已确认 PRD、本计划和基线报告。

- [ ] **Step 4: 用户授权后的可选提交门槛**

Stop and request explicit authorization before staging or committing. Only after authorization:

```powershell
git add friberg-game .github/workflows/friberg-game.yml README.md `
  docs/2026-09-07-v1.10-friberg-upstream-baseline.md `
  docs/2026-09-06-v1.10-player-question-bank-prd.md `
  docs/2026-09-06-v1.10-choice-quiz-mode-prd.md `
  docs/superpowers/plans/2026-09-06-v1.10-friberg-upstream-baseline.md
git status --short
git diff --cached --stat
git commit -m "feat: vendor friberg game baseline"
```

Expected: staged diff has no `.env`、压缩包、构建产物、日志、备份或生产配置。Do not push or create a PR without a separate explicit authorization.

---

## Phase 0 completion gate

阶段 0 只有在以下条件全部满足时才完成：

- `friberg-game/` 与固定提交源码一致，且无嵌套 Git 仓库；
- AGPL-3.0 和上游归属得到保留；
- 根 README 明确 MIT 与 AGPL 组件边界；
- Node 26 下冻结安装、测试和构建均有实际通过证据；
- 没有修改草人杯现有运行组件、Release 或部署；
- 基线报告记录所有已完成和未完成验证；
- 用户决定是否提交后，才能进入阶段 1。
