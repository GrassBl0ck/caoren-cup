# 草人杯 1.10 弗一把上游基线

## 固定来源

- 上游：`https://github.com/shnlfriberg/csgofriberg`
- 提交：`ad8439894c14a50fd1a40294e46942aa530c99cf`
- 上游提交时间：`2026-08-26T05:28:08Z`
- 快照导入日期：`2026-09-07`
- 许可证：GNU Affero General Public License v3.0（AGPL-3.0）
- ZIP SHA-256：`5285F26425DDA1A26F1F3F2684691F63B8A356D01324746300B5C4E5C52227D9`

## 导入范围

- 上游文件数：287
- 目标目录：`friberg-game/`
- 嵌套 Git 仓库：无
- 本地功能改动：无
- 草人杯集成改动：根 README 组件与许可证说明、独立 CI 工作流
- 尚未接入：草人杯导航、账号、题库改造、选择题、Release、部署

## 验证环境

- 操作系统：Windows
- 系统 PATH Node.js：v22.13.1
- Codex bundled Node.js：v24.19.0
- 本次便携 Node.js：v26.8.1
- 本次固定 pnpm：11.13.1
- Docker：不可用
- 本机 Redis：不可用

便携 Node.js 和固定 pnpm 位于当前工作树根部被 Git 忽略的 `node_modules/`。安装依赖时使用了经用户确认允许保留和使用的共享缓存：

- `D:\.pnpm-store\v11`
- `C:\Users\grassbl0ck\AppData\Local\node-gyp\Cache\26.8.1`
- `C:\Users\grassbl0ck\AppData\Local\npm-cache`

## 本地验证结果

### 冻结安装

- 命令：`pnpm install --frozen-lockfile`
- Node.js：v26.8.1
- pnpm：11.13.1
- 结果：通过
- 说明：锁文件通过供应链策略检查，安装 372 个包，`better-sqlite3` 使用 Node.js 26.8.1 成功编译。

### 完整测试

- 命令：`pnpm test`
- Node.js：v26.8.1
- pnpm：11.13.1
- 结果：未通过
- 测试文件：15 通过，19 失败，共 34 个
- 测试用例：60 通过，3 失败，97 跳过，共 160 项
- 原因：本机 `127.0.0.1:6379` 没有 Redis；18 个套件的 Redis 初始化钩子超时，`singleGameStore` 的 3 项测试超时。
- 判断：属于测试环境缺少上游规定的 Redis 7.4 服务，不能把本地结果记为基线通过。

### 完整构建

- 命令：`pnpm build`
- Node.js：v26.8.1
- pnpm：11.13.1
- 结果：通过
- PoW：预编译 WASM 与源码匹配，跳过 Rust 编译。
- 前端：Vite 6.4.3 成功转换 1982 个模块并生成 `client/dist/`。
- 后端：TypeScript 编译成功并生成 `server/dist/`。
- 警告：Vite 报告 `client/src/api/socket.ts` 同时被动态和静态导入；不影响本次构建退出码。

## GitHub Actions

- 工作流：`.github/workflows/friberg-game.yml`
- Node.js：26
- pnpm：11.13.1
- Redis：`redis:7.4-alpine`
- 冻结安装：待运行
- 完整测试：待运行
- 完整构建：待运行
- 状态：当前分支尚未推送包含本次快照的提交，等待提交后取得权威 CI 结果。

## 临时文件说明

- 临时归档位于 `.vendor-staging/csgofriberg-ad8439894c14a50fd1a40294e46942aa530c99cf.zip`。
- 执行环境连续拒绝删除操作，因此暂未清理。
- 该文件受根 `.gitignore` 的 `*.zip` 规则忽略，不会加入提交。
- 未使用替代删除方式绕过安全保护。

## 阶段结论

- 固定上游源码、来源记录、AGPL-3.0 边界和本地 Node.js 26 构建已经建立。
- 本地完整测试因缺少 Redis 7.4 未通过。
- 阶段 0 只有在 GitHub Actions 的 Node.js 26＋Redis 7.4 环境完成冻结安装、测试和构建后才能判定完成。
